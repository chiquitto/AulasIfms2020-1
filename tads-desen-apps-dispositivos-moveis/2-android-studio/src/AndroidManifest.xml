<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="br.com.chiquitto.android.buscacep">
  <!-- Definição de permissões -->
  <uses-permission android:name="android.permission.INTERNET" />
  <!-- Definição de icone, nome e tema (res/values/styles.xml) do app -->
  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:theme="@style/AppTheme">
    <activity android:name=".MainActivity">
      <intent-filter>
        <!-- MAIN indica que esta Activity pode ser executada como principal -->
        <action android:name="android.intent.action.MAIN" />
        <!-- LAUNCHER indica que o icone do app deve ficar disponível na Home -->
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
  </application>
</manifest>
